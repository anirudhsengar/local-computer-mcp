# Copy to runtime/tunnel.env, chmod 600, and fill locally. Never commit it.
CONTROL_PLANE_TUNNEL_ID=tunnel_0123456789abcdef0123456789abcdef
# Prefer a root-readable/user-readable 0600 file reference instead of a literal env value.
CONTROL_PLANE_API_KEY_FILE=/absolute/path/to/runtime-api-key
